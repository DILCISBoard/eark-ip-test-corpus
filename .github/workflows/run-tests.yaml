name: Runs pytest

on: [push]

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout corpus repository
        uses: actions/checkout@v4
        with:
          path: corpus
          ref: integration
      - name: Checkout eark-validator repository
        uses: actions/checkout@v4
        with:
          repository: E-ARK-Software/eark-validator
          path: validator
          ref: integration
      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install eark-validator
        run: |
          python -m pip install --upgrade pip
          pip install ./validator
      - name: Install corpus tests
        run: pip install -r ./corpus/tests/requirements.txt
      - name: Run corpus tests
        run: |
          cd corpus
          pytest --errorOnMissing