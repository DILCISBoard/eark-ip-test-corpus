<!-- Root element for an individual test case, allows these to be wrapped into
XML lists -->
<testCase xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="testCase.xsd"
  testable="TRUE">
  <!-- Unique ID for the test case -->
  <id specification="E-ARK CSIP" version="2.0.4" requirementId="CSIP100"/>
  <!-- URL references to requirements for convenient lookup -->
  <references>
    <reference requirementId="CSIP100" URL="http://earkcsip.dilcis.eu/#CSIP100"/>
  </references>
  <!-- The full text of the requirement. -->
  <requirementText>Schema file reference
    mets/structMap[@LABEL='CSIP']/div/div[@LABEL='Schemas']/fptr
    All file groups containing schemas described in the package are referenced via the relevant file group identifiers. One file group reference per fptr-element
</requirementText>
  <!-- Textual description of the test case, extra notes beyond the requirment text. -->
  <description>All fileGrps with the @USE="Schemas" must also be presented in the structMap. CSIP100 and CSIP118 are intrinsically linked together.</description>
  <!-- List of requirments that this test case depends on in addition to the
  main requirememt, e.g. general requirments on the form of IDs -->
  <dependencies>
    <dependency requirementId="CSIP118" URL="http://earkcsip.dilcis.eu/#CSIP118"></dependency>
  </dependencies>
  <!-- A list of the validation rules derived from the test case -->
  <rules>
    <rule id="1">
      <description>All fileGrps with the @USE="Schemas" must also be presented in the structMap via a fptr-element and an FILEID corresponding to the fileGrp-ID</description>
      <error level="ERROR">
        <message>All fileGrps with the @USE="Schemas" must be presented in the structMap via mets/structMap[@LABEL='CSIP']/div/div[@LABEL='Schemas']/fptr </message>
      </error>
      <corpusPackages>
        <package isValid="FALSE" isImplemented="TRUE" name="fileGrp_Schemas_but_missing_structMap">
          <path>invalid/fileGrp_Schemas_but_missing_structMap</path>
          <description>A package where there is a fileGrp with @USE="Schemas" but there is no structMap [@LABEL='CSIP']/div/div[@LABEL='Schemas']/fptr FILEID</description>
        </package>
        <package isValid="TRUE" isImplemented="TRUE" name="minimal_IP_with_1_representation">
          <path>valid/minimal_IP_with_1_representation</path>
          <description>Minimal IP</description>
        </package>
      </corpusPackages>
    </rule>
  </rules>
</testCase>
